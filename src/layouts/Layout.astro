---
import Header from '../components/Header.astro';
import Footer from '../components/Footer.astro';
import TerminalModal from '../components/TerminalModal.astro';
import SEO from '../components/SEO.astro';
import '../styles/global.css';

interface Props {
  title?: string;
  description?: string;
  image?: string;
  type?: 'website' | 'article';
  compactFooter?: boolean;
}

const { title, description, image, type, compactFooter = false } = Astro.props;
const publisherId = import.meta.env.PUBLIC_ADSENSE_CLIENT_ID || 'ca-pub-9242439217321222';
---

<!doctype html>
<html lang="ja">
  <head>
    <SEO title={title} description={description} image={image} type={type} />
    
    <!-- Google AdSense Verification & Auto Ads -->
    <meta name="google-adsense-account" content={publisherId} />
    <script
      async
      src={`https://pagead2.googlesyndication.com/pagead/js/adsbygoogle.js?client=${publisherId}`}
      crossorigin="anonymous"
    ></script>
  </head>
  <body>
    <Header />
    <main id="main-content">
      <slot />
    </main>
    <Footer compact={compactFooter} />
    <TerminalModal />
  </body>
</html>

<style>
  #main-content {
    flex: 1;
    display: flex;
    flex-direction: column;
  }
</style>
